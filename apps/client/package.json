{
  "name": "@web/client",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint --fix",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:cov": "jest --coverage",
    "test:ci": "jest --ci"
  },
  "devDependencies": {
  },
  "dependencies": {
    "@lib/fonts": "workspace:^",
    "@lib/styled-components-registry": "workspace:^",
    "@web/ui": "workspace:^"
  }
}
